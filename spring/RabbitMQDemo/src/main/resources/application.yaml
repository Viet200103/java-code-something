spring:
  application:
    name: RabbitMQDemo

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest



rabbitmq:
  queue:
    name: queue_demo
    json:
      name: json_queue_demo

  exchange:
    name: exchange_demo

  routing:
    key: routing_key_demo
    json:
      key: json_routing_key_demo
